<!DOCTYPE html>
<html lang="en-za">
<head>
    @{ await Html.RenderPartialAsync("_Declaration"); }
</head>
<body>
    @{ await Html.RenderPartialAsync("_PageTitle"); }
    <div id="body-100-percent">
        @RenderBody()
    </div>
    @{ await Html.RenderPartialAsync("_OnlineStoreFooter"); }
</body>
</html>
